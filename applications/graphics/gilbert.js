
class Gilbert {
    point(x, y) {
        return {x: x,
                y: y};
    }
    object(x, y) {
        let GilbertObject = function() {
            this.points = [];
            this.fill = "black";
            this.shader = () => {}
        };
        GilbertObject.prototype.render = function(x, y) {

        }
        return result;
    }
}